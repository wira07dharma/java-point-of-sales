/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package com.dimata.posbo.printing.kitchenorder;

import com.dimata.pos.entity.billing.Billdetail;
import com.dimata.pos.entity.billing.PstBillDetail;
import com.dimata.pos.entity.billing.PstBillMain;
import com.dimata.posbo.entity.masterdata.TableRoom;
import java.io.PrintWriter;
import java.util.Vector;
import javax.servlet.ServletConfig;
import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

/* Generated by Together */
public class PrintItemOrder extends HttpServlet {


    //public static String strURL = SessSystemProperty.PROP_APPURL;
    public void init(ServletConfig config) throws ServletException {

        super.init(config);

    }

    /** Destroys the servlet.

     */
    public void destroy() {
    }

    /** Processes requests for both HTTP <code>GET</code> and <code>POST</code> methods.

     * @param request servlet request

     * @param response servlet response

     */
    protected void processRequest(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, java.io.IOException {
        
//        long oidReceiveMaterial = FRMQueryString.requestLong(request, "hidden_receive_id");
//        String whereClauseItem = PstMatReceiveItem.fieldNames[PstMatReceiveItem.FLD_RECEIVE_MATERIAL_ID] + "=" + oidReceiveMaterial;
//        Vector listMatReceiveItem = PstMatReceiveItem.list(0, 0, whereClauseItem);
//        int multiLanguageName = Integer.parseInt((String)com.dimata.system.entity.PstSystemProperty.getValueIntByName("NAME_MATERIAL_MULTI_LANGUAGE"));
        
        String where ="";
        if (1==1){
            where = " cbm.TRANSACTION_STATUS=1 AND cbm.TRANSACTION_TYPE=0 AND cbm.DOC_TYPE=0 AND bd.STATUS=0 AND bd.STATUS_PRINT=0 ";
        }else{
            where = " lc.LOCATION_ID = 0"+
                    " AND cbm.TRANSACTION_STATUS=1 AND cbm.TRANSACTION_TYPE=0 AND cbm.DOC_TYPE=0 AND bd.STATUS=0 AND bd.STATUS_PRINT=0 ";
        }

        Vector listTransaction = PstBillMain.listItemOpenBill(0, 0,where, "");
    
        //String msgLoadString = "";
        response.setContentType("text/barcode");
        PrintWriter out = null;
        try {
            out = response.getWriter();
            for (int i = 0; i < listTransaction.size(); i++) {
                Vector dataBill = (Vector) listTransaction.get(i);
                Billdetail billDetail = (Billdetail)dataBill.get(0); 
                
                int xx = PstBillDetail.updateStatusPrintItem(billDetail.getOID(),1);
                
                TableRoom table =(TableRoom)dataBill.get(1);
                out.println(billDetail.getItemName()+"  -  "+billDetail.getQty()+" - T: "+table.getTableNumber());
                if(billDetail.getNote()!=""){
                    out.println(billDetail.getNote());
                    out.println("---------------------------------------------------");
                }else{
                    out.println("---------------------------------------------------");
                }
                
            }

        } catch (Exception exc) {
            System.out.println("" + exc);
        }

        out.flush();

    }

    /** Handles the HTTP <code>GET</code> method.

     * @param request servlet request

     * @param response servlet response

     */
    protected void doGet(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, java.io.IOException {

        processRequest(request, response);

    }

    /** Handles the HTTP <code>POST</code> method.

     * @param request servlet request

     * @param response servlet response

     */
    protected void doPost(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, java.io.IOException {

        processRequest(request, response);

    }

    /** Returns a short description of the servlet.

     */
    public String getServletInfo() {

        return "Short description";

    }


}

