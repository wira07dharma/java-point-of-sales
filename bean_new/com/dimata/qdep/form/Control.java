/* Generated by Together */

package com.dimata.qdep.form;

// import java

// import dimata
import com.dimata.util.*;

public class Control {

     public int start = 0;
     private int starting = 0;

     public int actionList(int listCmd, int start, int vectSize, int recordToGet) {
     
     switch(listCmd){                
         case Command.FIRST :                
             starting = 0;
             break;

         case Command.PREV :                
             starting = start - recordToGet;
             if(starting < 0){
                 starting = 0;
             }                
             break;

         case Command.NEXT :
             this.start = start + recordToGet;
             starting = start + recordToGet;
             if(starting >= vectSize){
                 starting = starting - recordToGet;
             }  
             
             break;
             
         case Command.LAST :
             int mdl = vectSize % recordToGet;
             if(mdl>0){
                 starting= vectSize - mdl;
             }
             else{
                 starting = vectSize - recordToGet;
             }                
             
             break;
             
         default:
             starting = start;
             if(vectSize<1)
                 starting = 0;                
             
             if(start > vectSize){
                 mdl = vectSize % recordToGet;
                 if(mdl>0){
                     starting = vectSize - mdl;
                 }
                 else{
                     starting = vectSize - recordToGet;
                 }                
             }                
             break;
     }
     return starting;
 }
}
